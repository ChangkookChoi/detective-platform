# 수집 출처 등록부

## 1. 목적

실제 출처의 운영 주체, 공개 경로, 구조, 이용 조건과 robots 확인 결과를 기록한다. 설정 파일이 형식 검증을 통과하거나 `robots.txt`가 허용한다고 해서 저작권·데이터베이스 권리와 관련 법령 검토가 모두 끝난 것으로 보지 않는다.

## 2. 등록 상태

| 상태 | 의미 |
| --- | --- |
| `pilot_approved` | 제한된 상세 URL과 사실 필드만 저빈도로 시험 수집 가능 |
| `manual_candidate` | 공식 출처와 최소 사실 필드는 확인했지만 자동 수집 구조·제한이 맞지 않아 관리자 수동 등록 전 재확인 |
| `deferred` | 구조 또는 허용 근거가 부족해 자동 수집하지 않음 |
| `blocked` | 약관·robots·접근 통제 또는 품질 문제로 자동 수집 금지 |

## 3. 등록 출처

### `mugunghwa-detective-official-pilot`

- 상태: `pilot_approved`
- 운영 주체: `탐정사무소 흥신소` 공식 홈페이지의 사업자 표기 운영자
- 정규 출처: `https://xn--mugunghwa-jd13bo06c.com/`
- 설정: [`../../services/collector/sources.toml`](../../services/collector/sources.toml)
- 확인일: 2026-07-23
- 확인자: Codex 보조 조사, 저장소 관리자의 위임 범위
- 정책 확인:
  - 홈페이지는 인증이나 접근 우회 없이 공개되어 있다.
  - 일반 이용약관이나 자동 접근·사실 정보 재사용을 금지하는 문구는 홈페이지에서 확인되지 않았고 개인정보처리방침만 연결되어 있다.
  - 페이지의 저작권 있는 본문·이미지는 수집하지 않고 JSON-LD의 상호, 사업상 공개 대표전화와 주소만 후보로 추출한다.
  - 업체 소개 `description`은 홍보 문구와 저작물 복제 위험을 줄이기 위해 허용 필드에서 제외한다.
- robots 확인:
  - [`robots.txt`](https://xn--mugunghwa-jd13bo06c.com/robots.txt)는 `User-agent: *`, `Allow:/`를 제공한다.
  - 허용 경로는 홈페이지 `/` 하나로 제한한다.
- 구조 확인:
  - 홈페이지는 `ProfessionalService` JSON-LD 한 건에 상호, 대표전화와 `PostalAddress`를 제공한다.
  - 2026-07-23 확인 시 HTTP 200, 최종 URL 동일, 응답 약 37KB였다.
- 실행 제한:
  - 동시성 1, 요청 간격 10초, 최대 2회 시도, 응답 100KB, redirect 1회
  - 명시적 User-Agent와 저장소 이슈 연락 경로 사용
  - 실제 실행 전 robots와 페이지 구조를 다시 확인하고 첫 실행은 개발 DB 한 건으로 제한
- 중단 조건:
  - robots 또는 이용 조건 변경, `403`·`429`, 구조화 데이터 제거·복수 후보화, 사업 연락처 여부 불명확, 운영자 요청

이 등록은 사이트 운영자의 별도 서면 허락을 의미하지 않는다. 단일 공식 페이지의 공개 사실 필드를 검수 후보로만 만드는 기술·운영 파일럿이며, 공개 반영은 별도 관리자 검수를 거친다.

#### 최초 개발 DB 실행

- 실행일: 2026-07-23
- 환경: 저장소 전용 지속형 PostgreSQL 17 개발 DB, 수집기 최소 권한 역할
- 결과: `succeeded`, 발견 1건, 수집 1건, 실패 0건
- 검수 경계: `pending/new_office/high` 1건 생성, 기존 운영 업체 행 수정·생성 0건
- 공개 상태: 미승인·미공개
- 확인 범위: 실행 상태와 건수, 검수 유형·위험도 및 역할 권한만 기록했으며 추출된 연락처·주소 값은 로그나 문서에 복제하지 않음

이 실행은 기술적 적재와 검수 경계 확인이며 사이트 운영자의 별도 서면 허락이나 플랫폼 공개 승인을 대신하지 않는다. 다음 단계에서 인증된 관리자가 원문과 제안값을 직접 비교하고 별도 근거를 확인해야 한다.

#### 로그인 전 조건부 재확인

- 재확인일: 2026-07-23
- 정책 확인: `robots.txt`의 전체 허용 규칙과 공개 홈페이지 접근 상태 유지, 출처 설정 검증 통과
- 실행 결과: `succeeded`, 발견 1건, 수집 1건, 변경 없음 1건, 신규 검수 0건, 실패 0건
- 실행 ID: `7d0a5419-852a-4d7e-b856-a2d0d53aa111`
- 검수 경계: 기존 `pending/new_office/high` 1건, 감사 이력 0건, 운영 업체 0건 유지

조건부 요청으로 원문 후보가 최초 적재 시점과 동일함을 확인했지만, 이 결과만으로 업체 정보의 사실성이나 공개 적합성을 승인하지 않는다.

#### `jsonld-v2` 주소 정규화 교정 실행

- 실행일: 2026-07-23
- 실행 전 확인: 공식 홈페이지 HTTP 200, `robots.txt`의
  `User-agent: *`·`Allow:/` 유지, 출처 설정 검증 통과
- 변경 이유: `jsonld-v1`이 전체 도로명 주소에 이미 포함된 지역명을 다시
  붙여 주소 지역명이 중복되는 품질 결함을 확인
- 실행 결과: `succeeded`, 발견 1건, 수집 1건, 신규 검수 1건, 실패 0건
- 실행 ID: `c68c478e-7a0f-4850-b7df-ef17b8ca5196`
- 검증 결과: 실행·수집 레코드 버전 `jsonld-v2`, 정규화 주소와 제안 주소가
  교정 기대값에 일치
- 검수 경계: 교정 후보 `pending/new_office/high` 1건 생성, 기존
  `jsonld-v1` 후보는 `on_hold`와 감사 이력 1건을 유지
- 공개 상태: 운영 업체 0건, 미승인·미공개

추출기 버전이 바뀐 첫 실행은 이전 버전의 ETag·콘텐츠 해시를 재사용하지
않아 원문을 다시 확인하고 교정 후보를 생성했다. 기존 보류 후보는 로그인한
관리자가 결함 사유로 반려하기 전까지 보존한다. 두 후보 중 어느 것도 자동
승인하거나 공개하지 않는다.

## 4. 자동 수집 제외·수동 후보

| 후보 | 확인 결과 | 결정 |
| --- | --- | --- |
| `www.genie-world.co.kr` | robots 전체 허용과 공식 사업자·서울 금천 소재 표시는 확인했으나 지원 가능한 사업체 JSON-LD가 없음 | `manual_candidate` — JavaScript 번들 전용 어댑터를 만들지 않고 수동 재확인 |
| `daok.kr` | robots 전체 허용과 공식 사업자·서울 송파 소재 표시는 확인했으나 지원 가능한 JSON-LD가 없음 | `manual_candidate` — 사이트별 DOM 파서를 만들지 않고 수동 재확인 |
| `www.apmtwo.com` | robots는 공개 경로를 허용하지만 `OnlineStore` JSON-LD 주소가 `-`로 제공되어 운영 정보 후보로 부적합 | `deferred` — 화면 본문을 별도 파싱하지 않음 |
| `xn--jk1bs41btlgz1e.com` | `robots.txt`가 404이고 지원 가능한 JSON-LD가 없음 | `deferred` — 허용과 구조가 모두 불명확 |
| `sonamoo.or.kr` | 공식 사업자 표기와 경기 화성 소재지는 확인했으나 정책 실행 환경에서 DNS 재확인이 불가능함 | `deferred` — 자동 등록하지 않고 수동 후보 검토 |
| `tamjung.mgdetective.co.kr` | 공개 경로의 robots 허용과 공식 사업자·경기 부천 소재 표시는 확인했으나 지원 가능한 사업체 JSON-LD가 없음 | `manual_candidate` — 공식 원문을 수동 재확인 |
| `htamjung.com` | 공식 사업자 표기와 경기 안산 소재지는 확인했으나 정책 실행 환경에서 DNS 재확인이 불가능함 | `deferred` — 자동 등록하지 않고 수동 후보 검토 |
| `jeongtam.com` | 공식 사업자 표기와 경기 화성 소재지는 확인했으나 유효한 robots 정책과 지원 JSON-LD가 없음 | `deferred` — 공식 원문을 수동 후보로 검토 |
| `xn--m01bq5ku5ay6xjtk.com` | 공개 경로의 robots 허용과 공식 사업자·경기 시흥 소재 표시는 확인했으나 JSON-LD 유형이 현재 지원 범위 밖임 | `manual_candidate` — 공식 원문을 수동 재확인 |
| `leeandjang.net` | 공식 사업자·서울 종로 소재와 대표 전화는 확인했으며 robots는 전체 허용이지만 정책 실행 환경의 본문 요청이 실패하고 지원 JSON-LD가 없음 | `manual_candidate` — 브라우저 원문을 수동 재확인 |
| `hositamtam.kr` | 공식 사업자·경기 의정부 본사와 대표 전화, 공개 경로 robots 허용을 확인했으나 응답이 약 793KB이고 `OnlineStore` JSON-LD만 제공 | `manual_candidate` — 대용량 Imweb 본문을 자동 수집하지 않고 수동 재확인 |
| `xn--z52bt3m.com` | 공식 사업자·경기 고양 소재와 대표 전화는 확인했으나 응답이 약 1.2MB로 현재 한도를 넘고 접근 환경별 403 및 `Organization` 중심 JSON-LD가 확인됨 | `manual_candidate` — 접근 상태와 원문을 브라우저에서 다시 확인 |
| `tracker.imweb.me` | 공식 사업자·서울 강남 소재와 대표 전화, 공개 경로 robots 허용을 확인했으나 응답이 약 764KB이고 `OnlineStore` JSON-LD만 제공 | `manual_candidate` — 대용량 Imweb 본문을 자동 수집하지 않고 수동 재확인 |
| `xn--2z1b33o18ft2cdrq3lc.com` | 공식 법인·서울 서초 소재와 대표 전화, robots 전체 허용을 확인했으나 지원 가능한 JSON-LD가 없음 | `manual_candidate` — 사이트별 DOM 파서를 만들지 않고 수동 재확인 |

위 후보는 모두 `sources.toml`에 넣지 않으며 자동으로 요청하지 않는다.
2026-07-24 추가 묶음은 공개 홈페이지를 한 번씩 확인하고 상담 폼, 사례
본문과 이미지를 수집 대상에서 제외했다. `manual_candidate`도 등록 승인이
아니며 실제 입력 직전에 공식성, 대표번호, 한 개의 정확한 사무소 주소와
이용 조건을 다시 확인한다.

공식 운영 주체와 최소 사실 필드를 사람이 재확인한 `manual_candidate`는
인증된 관리자 화면 `/admin/reviews/new`에서 고위험 신규 검수 후보로 등록할
수 있다. 등록자는 정확한 한 개 사무소임과 사건·상담·개인 연락처 등
민감정보가 없음을 필수 확인한다. 이 경로도 업체를 자동 생성·공개하지 않으며
제출자 ID와 출처 URL을 감사 정보로 남긴다. 이용 조건·공식성·필수 필드를
확인하지 못한 후보는 수동 등록도 하지 않는다.

## 5. 재검토

- 파일럿 실행 직전과 운영 예약 실행 도입 전
- robots, 이용약관, 정규 도메인 또는 운영 주체 변경 시
- 90일 이상 정책 확인이 갱신되지 않았을 때
- 정정·삭제 요청이나 차단 신호를 받았을 때

재검토 결과는 확인일·확인자와 함께 이 문서와 설정 파일에 반영한다.
