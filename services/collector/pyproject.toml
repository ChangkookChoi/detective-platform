[project]
name = "collector"
version = "0.1.0"
description = "Policy-bound office information collection and change detection"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx==0.28.1",
    "psycopg[binary]==3.3.4",
]
